name: Build and deploy to GitHub Pages

# Trigger on pushes to v2.0 (change if you want deploy from another branch)
on:
  push:
    branches:
      - v2.0

jobs:
  build-and-upload:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload artifact for GitHub Pages
        # use v3 of the upload-pages-artifact action (uses updated artifact infra)
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./build

  deploy:
    needs: build-and-upload
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    steps:
      - name: Deploy to GitHub Pages
        # use v4 of deploy-pages which works with the new artifact action
        uses: actions/deploy-pages@v4
